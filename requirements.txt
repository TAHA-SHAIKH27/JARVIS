# ── Core web server / API ──────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.9
pydantic==2.13.4

# ── HTTP & configuration ──────────────────────────────────────────
requests==2.34.1
python-dotenv==1.0.1
PyYAML==6.0.3

# ── AI / cloud service integrations ───────────────────────────────
google-auth==2.56.2
google-auth-oauthlib==1.2.1
huggingface_hub==1.24.0

# ── Document / report / chart generation ──────────────────────────
python-docx==1.1.2
python-pptx==1.0.2
pdfplumber==0.11.10
Pillow==12.3.0
matplotlib==3.11.2
numpy==2.5.1

# ── Browser automation (Chromium via Playwright) ──────────────────
playwright==1.62.0

# ── System monitoring / telemetry ─────────────────────────────────
psutil==6.1.1

# ── Windows-only desktop automation ───────────────────────────────
PyAutoGUI==0.9.54; sys_platform == "win32"
pyperclip==1.11.0; sys_platform == "win32"
uiautomation==2.0.18; sys_platform == "win32"
pywinauto==0.6.8; sys_platform == "win32"
pywin32==312; sys_platform == "win32"

# ── Optional: OCR for screenshots (also requires the Tesseract binary) ──
pytesseract==0.3.13